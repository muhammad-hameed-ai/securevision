package com.securevision.feature.dashboard

/**
 * Navigation identity of the dashboard feature.
 *
 * Phase 2 adds `DashboardScreen` and `DashboardViewModel` alongside this file:
 * the stat cards for enrolled profiles, unread alerts, recordings and total
 * detections, plus the most recent alerts.
 */
object DashboardRoutes {

    /** The app's home destination once signed in. */
    const val DASHBOARD = "dashboard"
}
